import 'package:flutter/material.dart';

class CoAppbar extends StatelessWidget {
  const CoAppbar({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
     
    );
  }
}
